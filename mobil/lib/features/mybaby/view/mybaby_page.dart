import 'package:flutter/widgets.dart';

class MyBabyPage extends StatelessWidget {
  const MyBabyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Baby Page'),
    );
  }
}
